/*
// End
const last = numbers.pop();
console.log(numbers);
console.log(last);


// Beginning
numbers.shift();
console.log(first);

// Middle
numbers.splice()







const numbers = [1, 2, 3, 4]


// End
const last = numbers.pop();

// Beginning
const first = numbers.shift();
console.log(first);

// Middle
numbers.splice()



*/


const numbers = [1, 2, 3, 4]


// End
const last = numbers.pop();

// Beginning
const first = numbers.shift();


// Middle
numbers.splice(2, 2);
console.log(numbers)